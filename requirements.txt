# NorthPeak Analytics — Python dependencies
# Phase 1 (ingestion + warehouse) needs only the first block.
# Later phases' deps are listed but commented so `pip install -r` stays fast for now.

# --- Phase 1: ingestion + warehouse ---
duckdb==1.5.4
Faker==40.31.0
pandas==2.3.3

# --- Phase 2-3: transform (dbt) ---
# dbt-core==1.9.*
# dbt-duckdb==1.9.*

# --- Phase 4: data quality ---
# great-expectations==0.18.*

# --- Phase 5-6: orchestration + alerting ---
# dagster==1.9.*
# dagster-webserver==1.9.*
