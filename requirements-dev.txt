# NorthPeak Analytics — full dev / CI dependency set (all phases).
#
# requirements.txt keeps later-phase deps commented so a Phase-1-only `pip install`
# stays fast. This file is the opposite: every dependency the pipeline, tests, and
# quality suite need, pinned to versions verified green end-to-end (generate ->
# load -> dbt build + 73 tests -> 25 quality checks). CI installs from here.

# --- ingestion + warehouse + synthetic fixtures ---
duckdb==1.5.4
pandas==2.3.3
Faker==40.31.0

# --- transform (dbt) ---
dbt-core==1.12.0
dbt-duckdb==1.10.1

# --- data quality (Great Expectations 1.x fluent API) ---
great-expectations==1.20.0
